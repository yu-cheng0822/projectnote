# note
## Securing Agents With Tracked Capabilities 翻譯
introduction
基於大型語言模型 (LLM) 構建的 AI 代理正在透過呼叫軟體工具來實現目標，從而變革業務流程和軟體開發。工具透過諸如 MCP [ 5 ] 之類的協定暴露給代理，代理的功能可以透過可重複使用技能進行擴展 [8 ]。一個很有前景的方法是程式碼執行 [ 7 , 34 ]：代理不再發出單一工具調用，而是產生程式片段，這些程式片段一步完成多個工具和控制邏輯的組合，從而減少令牌的使用，並使工具的使用更加明確和可審計。問題在於，目前的代理設計引入了嚴重的安全風險。由於錯置、提示注入 [ 24 ]、幻覺或簡單的錯誤 [ 4 ]，代理可能會濫用工具，造成損害或洩露私有資訊。工具呼叫通常在缺乏人工監督的情況下自動進行 [ 29 ]，因此 AI 產生的計畫可能會在缺乏充分監督的情況下執行大規模操作。編碼代理程式（例如 Claude Code 或 GitHub Copilot）通常被授予對整個專案目錄的存取權限，其中可能包含 API 金鑰、憑證或機密文件等敏感資訊。當代理人依賴第三方模型時，此類資料可能會傳輸給不受信任的提供者 [59]。現有的防禦措施有許多限制 [48]。基於模式的權限規則（路徑或指令的允許清單或封鎖清單）粒度過粗，無法捕捉上下文相關的策略。互動式確認會要求使用者批准風險操作，但這會導致使用者產生確認疲勞，並且在實踐中往往被忽略。關鍵在於，這兩種方法都無法對資訊流提供強有力的保證：即使每個步驟都獲得了許可，代理也可能在某個步驟中讀取敏感信息，然後在後續步驟中將其竊取。我們提出了一種“安全機制”，該機制將代理的行為限制在一系列可證明安全的操作範圍內，同時保持其有效的表達能力。核心挑戰在於平衡安全性和實用性：過於嚴格的控制會阻礙合法工作，而過於寬鬆的策略則會留下安全漏洞。我們的關鍵洞見在於，透過系統地使用追蹤能力，可以建構兼具安全性和表達力的框架。能力是一種成熟的安全原語，已被應用於 Hydra [72] 和 Fuchsia [30] 等作業系統以及 CHERI [70] 等硬體中。我們採用物件能力 [26, 44] 的原始形式。最近的研究 [13, 75] 表明，如果將能力以靜態類型跟踪，則可以顯著提高能力的表達力。我們提供了一個實作方案 tacit（類型中的追蹤代理能力），它是一個基於 Scala 3.1 建構的開源 MCP 伺服器。為什麼需要類型和能力？定制的類型系統可以在代理程式運行之前捕獲錯誤，不會增加監控開銷，並且能夠提供適用於所有輸入（而不僅僅是經過測試的輸入）的保證。它還具有模組化特性：代理環境可以由可重複使用的組件組裝而成，這些組件的安全性源自於類型層級的屬性。原則上，完整的形式化規範可以使代理行為可驗證，但精確的規範通常不可用或不切實際。類型是一種輕量級的折衷方案，其挑戰在於如何在簡潔性（更易於理解）和表達力（更強的安全屬性）之間取得平衡。能力則添加了類型本身無法實現的功能：細粒度的、上下文相關的權限，這些權限在類型中反映出來後，還可以描述效果 [13] 並區分純粹的計算和副作用計算，適用於控制動作和資訊流。物件能力在概念上很簡單，因為它們是普通的程式值，在類型中對其進行註釋只需要少量額外的符號（類似於 Scala 和 DOT 中的路徑依賴類型 [ 3, 60 ]）。傳統的能力架構很麻煩，因為它們需要透過呼叫鏈傳遞所有能力。在附錄中，我們展示瞭如何使用全域能力和隱式參數來表示追蹤能力，從而減輕這種負擔。本文的其餘部分組織如下：第 2 節介紹了 Scala 3 中的追蹤能力；第 3 節解釋了它們如何用於防止代理程式碼中的信息洩露，並介紹了隱式實現；第 4 節展示了驗證 LLM 可以生成與我們類型匹配的代碼，以及生成的代碼可以防止特定類型的不安全行為的實驗；第 5 節可以生成與我們類型匹配的代碼，以及生成的代碼可以防止特定類型的不安全行為的實驗；第 5 5 節與我們的方法，總結了第7 節；

重點 : 
我們提出了一種“安全機制”，該機制將代理的行為限制在一系列可證明安全的操作範圍內，同時保持其有效的表達能力。核心挑戰在於平衡安全性和實用性：過於嚴格的控制會阻礙合法工作，而過於寬鬆的策略則會留下安全漏洞。我們的關鍵洞見在於，透過系統地使用追蹤能力，可以建構兼具安全性和表達力的框架。能力是一種成熟的安全原語，已被應用於 Hydra [72] 和 Fuchsia [30] 等作業系統以及 CHERI [70] 等硬體中。我們採用物件能力 [26, 44] 的原始形式。

# 2. 追蹤能力（Tracked Capabilities）入門

簡單來說，**能力（Capability）**是一種「具有特殊權限或價值」的物件，例如**檔案控制代碼（File Handle）**、**存取權杖（Access-Permission Token）**或**可變資料結構（Mutable Data Structure）**，通常與程式的**副作用（Effects）**及**權限（Permissions）**有關。

在 Scala 中，能力本質上就是一般物件，它們透過自身的方法授予相應的操作權限，並且可以像一般物件一樣被傳遞或回傳。

例如，一個 `FileSystem` 能力可以提供檔案存取功能：

```scala
def writeOutput(fs: FileSystem^) =
  val f = fs.access("OUTPUT.md") // FileEntry capability
  f.write("The answer is 42.")
```

---

## 捕獲型別（Capturing Types）

Scala 3 會在**型別（Type）**中追蹤能力。

除了描述值本身的型別之外，型別還會記錄該值可能**捕獲（Capture）**的能力。

捕獲型別（Capturing Type）的形式如下：

```text
T^{x1, ..., xn}
```

其中，捕獲集合（Capture Set）`{x1, ..., xn}` 是對此型別值可能存取能力的**保守近似（Over-Approximation）**。

例如，下列閉包（Closure）：

```scala
(s: String) => f.write(s)
```

其型別為：

```text
String ->{f} Unit
```

這是

```text
(String -> Unit)^{f}
```

的簡寫。

此型別明確表示：這個函式會使用能力 `f`。

### 純型別與一般型別

若捕獲集合為空，只寫成：

```text
T
```

代表它是一個**純（Pure）**型別，不保留任何能力。

另一方面，

```text
T^
```

是

```text
T^{any}
```

的簡寫，表示此值可能保留**任意能力**。

Scala 編譯器會自動推導（Infer）並檢查這些能力。任何程式碼若試圖使用**未在型別中宣告的能力**，都會在編譯時被拒絕，而不是等到執行時才發現錯誤。

---

## 區域純性（Local Purity）

由於能力會記錄在型別系統中，因此 Scala 可以在**編譯階段（Compile Time）**保證程式的純性（Purity）。

本研究將敏感資料包裝成 `Classified` 型別，其 `map` 方法只接受**純函式（Pure Function）**：

```scala
trait Classified[+T]:
  def map[U](op: T -> U): Classified[U]
```

其中

```text
T -> U
```

表示 `op` **不能捕獲任何能力（Capability）**。

因此，它不能：

- 寫入檔案
- 發送網路請求
- 執行任何其他具有副作用（Side Effect）的操作

### 合法範例

```scala
val secret: Classified[String] =
  readClassified("key.txt")

secret.map(s => s.toUpperCase)
```

此程式碼只是將字串轉成大寫，沒有使用任何能力，因此可以正常編譯。

### 非法範例

```scala
secret.map {
  s =>
    f.write(s)
    s
}
```

此閉包會捕獲能力 `f`，因此其推導出的型別為：

```text
String ->{f} String
```

然而，`map` 所要求的是純函式：

```text
String -> String
```

兩者型別不相容，因此編譯器會直接拒絕。

也就是說，**機密資料可以參與運算，但不能透過副作用洩漏出去**。

---

## 生命週期控制（Lifetime Control）

捕獲檢查（Capture Checking）還可以透過 **with-resource** 模式控制能力的生命週期：

```scala
def requestFileSystem[T](root: String)
    (block: FileSystem^ => T): T
```

執行流程如下：

1. 建立一個 `FileSystem` 能力。
2. 將它傳入 `block`。
3. `block` 執行完後，該能力立即失效。

### 能不能偷偷把能力帶出去？

例如：

```scala
val bad =
  requestFileSystem("/data"):
    fs =>
      () =>
        fs.access("secret.txt").read()

bad()
```

乍看之下，似乎可以把 `fs` 包在閉包（Closure）中帶到外部。

但實際上，這段程式**無法通過編譯**。

原因如下：

- 該閉包的型別會在捕獲集合中包含 `fs`。
- `block` 的回傳型別 `T` 是**區域（Local）型別**，不能引用區域能力 `fs`。
- 因此，編譯器會拒絕任何仍保留 `fs` 能力的值。

這些值包括：

- 閉包（Closure）
- 容器（Container）
- 外部變數（External Variable）

也就是說，**能力不能被偷偷帶離它的有效生命週期**。

---

## 本節重點整理

### Tracked Capabilities 的核心概念

- Capability 是具有特定權限的物件，例如檔案系統、網路或可變資料結構。
- Scala 3 將 Capability 記錄在型別（Type）中。
- 型別除了描述資料型態，也描述可能使用哪些 Capability。
- 編譯器會檢查所有 Capability 是否符合型別宣告。

### Capturing Types

- `T`：純型別（Pure），不保留任何能力。
- `T^`：可能保留任意能力。
- `T^{f}`：表示值會捕獲能力 `f`。

### Local Purity

- `Classified` 只允許純函式操作敏感資料。
- 純函式不能存取檔案、網路等具有副作用的能力。
- 因此可避免機密資料外洩。

### Lifetime Control

- Capability 只能存在於指定作用範圍（Scope）。
- 一旦離開 Scope，Capability 就會失效。
- 編譯器禁止任何方式將 Capability 帶出 Scope。

---

## 小結

Tracked Capabilities 是 Scala 3 提供的一種能力追蹤機制，其特色包括：

- 在型別中記錄能力（Capability）。
- 編譯期間檢查能力是否合法使用。
- 保證純函式（Pure Function）不會產生副作用。
- 防止敏感資訊透過副作用洩漏。
- 提供細粒度（Fine-Grained）的能力生命週期管理。
- 避免能力被偷偷帶出其作用範圍，提高程式安全性。

---
## 5討論（Discussion）
非目標與限制（Non-goals and limitations）我們的框架確保代理（agent）生成的程式碼僅執行安全的運作，但它並未解決所有可能的失敗模式。我們識別出以下非目標：正確性（Correctness）：我們的目標並非針對模型的幻覺（hallucination）或邏輯錯誤。型別系統確保了運作是安全的（例如：無未授權存取與無資訊洩漏），但代理仍可能產生錯誤的結果。確保代理輸出的正確性與我們的安全性保證是正交的（orthogonal）。側通道（Side channels）：我們不涵蓋諸如時間或終止等隱蔽通道（covert channels）。例如，代理原則上可以透過在呼叫 map 函數時，根據機密資料的內容選擇花費不同長短的時間，來洩漏單一位元（bit）的機密資訊。外部指令的安全性（Safety of external commands）：由代理生成的外部處理程序（processes）會逃逸出 Scala 安全模式的邊界，因此靜態保證會退化為底層白名單（allowlist）的保證。在實務上，白名單應限制在特定的安全指令（例如 git diff、sbt），且重視安全的部署應將靜態機制與沙箱（sandboxing）結合，以達到縱深防禦（defense in depth）。能力可以作為工具設計者、管理者與使用者之間的一種通用語言，用於在外部工具上表達更細粒度的權限。代理是否為對抗性的？（Are agents adversarial?）我們的 harness 在系統安全的意義上，將代理視為對抗者（adversaries）。有人可能會認為這過於嚴格：畢竟，前沿模型已經能夠抵抗許多提示詞注入（prompt-injection）攻擊（表 1）。然而，間接提示詞注入仍可能誘使代理做出有害的行為 [24, 78]。例如，Rehberger [59] 透過 Anthropic 的 Files API 滲透了使用者資料，而 Check Point [18] 則透過惡意的儲存庫設定實現了無聲的指令執行。其次，我們必須假設任務自動化將變得極為普及，以至於數百甚至數千個代理將在大體上無人監督（unsupervised）的情況下執行業務流程。在這種環境下，簡單的失誤極有可能導致嚴重的漏洞。為了防止不想要的行為，我們應該依賴透過模組化推理（modular reasoning）來強制執行的合約，使用強表達力的型別系統與基於能力的架構，並遵循最低特權原則（principle of least privilege）[63]。一種相似但野心較小的作法被稱為安全編碼（safe coding）[37]，已在擁有大量程式設計人員的組織中成功採用。其他方法（Other approaches）Scala 3 目前是唯一具備靜態追蹤能力且達到生產就緒（production-ready）的語言，這也是我們選擇它作為基礎的原因。每當能夠表達能力安全、在型別中反映能力以及區域純粹性（local purity）時，這些技術就可以推廣到其他環境中；我們在附錄 F 中討論了其他替代基礎（其他主流語言、Haskell、E/Joe-E、自訂 DSL）。實務採用（Practical adoption）部署我們的作法需要 (1) 建構 harness 基礎設施，以及 (2) 訓練代理來生成具型別的程式碼。編寫基礎設施（能力函式庫、Classified 包裝器、工具外觀 [facades]）需要熟悉 Scala 3 的捕獲檢查。這是一次性成本：該基礎設施有兩層，且只有底層需要 Scala 專業知識。可重用的基礎設施（能力函式庫、Classified 包裝器、受信任的 LLM 對話）大約有 1,000 行 Scala 程式碼，編寫一次即可在所有代理和領域中共用。各領域的外觀與現有的 JSON 工具綱要（schemas）一一對應，主要由 case class 定義和方法簽章組成：$\tau^2$-bench 的航空與零售外觀各為數百行（參見附錄 H.3），其大小與它們所取代的 JSON 綱要相當。將 Agent-Dojo 的四個現有領域進行移植，除了生成具型別的外觀之外，不需要對使用者任務或攻擊進行任何更改。因此，特定任務的環境建構成本相對便宜，且代理本身也可以協助從現有的工具綱要生成外觀，因為環境在調用代理之前會先經過人類審查。一個更根本的問題是，鑑於 Scala 3 的捕獲檢查 [65] 是一項在訓練資料中代表性稀少的實驗性功能，當前的 LLM 是否能夠生成帶有追蹤能力的 Scala 3 程式碼。我們的實驗（第 4.2 節）表明答案是肯定的：代理的表現與工具呼叫基準（tool-calling baselines）相當，因為型別標註是輕量級的，且程式碼模式足夠重複，足以透過系統提示詞與少數範例來學習。針對訓練資料中代表性不足的語言的後訓練技術 [14] 可以進一步縮小差距。編譯負擔（Compilation overhead）適中：Scala 3 編譯器能在不到一秒的時間內完成典型代理程式碼片段的型別檢查，與 LLM 推論延遲相比微不足道。多代理情境（Multi-agent scenarios）多代理協同編排（Multi-agent orchestration）在實務上的部署日益增加 [9]。我們的能力模型可以自然地擴充：外部代理可以授予子代理其自身能力的一個子集，而編譯器會靜態拒絕任何超出此預算（budget）的子代理程式碼。代理間的通道本身也可以被建模為能力。我們將完整的的多代理評估留待未來的工作。 
